# Engine API

The following will contain all documentation and source information on all classes, functions, and more, in the engine.

`Runtime` - The code that's included in the compiled program, everything besides external tools will use this

`Editor`
