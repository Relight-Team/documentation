# MathUtil

MathUtil contains utilities for math.

## Functions

```cpp
RMath::Max(const T A, const T B)
```
returns the greater of 2 values, T is a template that can be any value that supports being compared
