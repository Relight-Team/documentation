{"config":{"lang":["en"],"separator":"[\\s\\-]+","pipeline":["stopWordFilter"],"fields":{"title":{"boost":1000.0},"text":{"boost":1.0},"tags":{"boost":1000000.0}}},"docs":[{"location":"","title":"Home","text":"<p>Working Progress Engine</p> <p>Relight Engine is currently is very active development and thus is actively changing. Please do not write any projects at this time as everything is subject to change</p> <p>Relight Engine is a free and open source gaming and 3D application engine created by Relight Team. The engine takes insparation of the inner workings of Unreal Engine</p> <p>Getting Started - Learn the basics of creating a game from scratch!</p> <p>Engine API - Documentation on API, includes Runtime and Editor</p> <p>Dealing with Content - Learn how to use all sorts of Assets, configuration, and shading in Relight Engine</p> <p>Tools and Programs - Documentation on tools and programs for Relight Engine</p> <p>Plugins - Learn how to use and create your own runtime/editor plugins to enhance development and your game</p>"},{"location":"#download-and-socials","title":"Download and Socials","text":"<p>Engine Github</p> <p>Documentation Github</p>"},{"location":"Engine%20API/","title":"Engine API","text":"<p>The following will contain all documentation and source information on all classes, functions, and more, in the engine.</p> <p><code>Runtime</code> - The code that's included in the compiled program, everything besides external tools will use this</p> <p><code>Editor</code></p>"},{"location":"Engine%20API/Runtime/ApplicationCore/","title":"ApplicationCore","text":"<p>Not done, please add later once ApplicationCore is complete</p>"},{"location":"Engine%20API/Runtime/Core/","title":"Core module","text":"<p>The Core module contains common code that will be used everywhere in the engine.</p>"},{"location":"Engine%20API/Runtime/Core/#notes","title":"Notes","text":"<ul> <li> <p>Core has multiple include collections</p> <ul> <li> <p><code>Core.h</code> contains every core file, including other collections</p> </li> <li> <p><code>CoreMinimal.h</code> contains minimal includes that is contains the basic elements of the engine</p> </li> <li> <p><code>CoreTypes.h</code> contains unique values the engine will constantly use, for example, Arrays and Strings</p> </li> <li> <p><code>Platform.h</code> contains includes of all platform-specific classes and functions</p> </li> <li> <p><code>PlatformCore.h</code> contains important code and includes needed for almost every file</p> </li> </ul> </li> <li> <p>Relight Engine uses the name <code>UTF16</code> to define UTF-16 characters</p> </li> <li> <p>Platforms are imported based on each folder based on their platform name, all platforms uses <code>PlatformBase</code> as their parent class</p> </li> </ul>"},{"location":"Engine%20API/Runtime/Core/Algorithm/Sort/","title":"Sort","text":"<p>The Sort function handles any array type to organize and put each value in order. To access the function</p> <p>Sort is defined in CoreMinimal.h</p>"},{"location":"Engine%20API/Runtime/Core/Algorithm/Sort/#run-sort","title":"Run Sort","text":"<p>The Sort function is defined below:</p> <pre><code>void Algo::Sort(ArrayType&amp; Input, ArrayAlgo Type = ArrayAlgo::IntroSort)\n</code></pre> <p>The ArrayType is a template that can be replaced with any container that supports <code>Swap()</code>, <code>Replace()</code>, and <code>Indices()</code> function</p> <p>The Type contains the algorithm we will use for the sort algorithm we will use, the default algorithm is IntroSort (introspective sort)</p> <p>Here's all the options in ArrayAlgo that you can use:</p> <pre><code>ArrayAlgo::IntroSort // Introspective Sort\n\nArrayAlgo::HeapSort // Heap Sort\n\nArrayAlgo::InsertionSort // Insertion Sort\n\nArrayAlgo::QuickSort // Quick Sort\n</code></pre>"},{"location":"Engine%20API/Runtime/Core/Containers/Array/","title":"Array","text":"<p>The <code>Array</code> class is Relight Engine's custom dynamic array system that is stored in the heap. An array contains an ordered collections of a specific variable type.</p> <p>It can also be included in <code>CoreTypes.h</code> and <code>CoreMinimal.h</code></p>"},{"location":"Engine%20API/Runtime/Core/Containers/Array/#constructor","title":"Constructor","text":"<p>Template</p> <p>You must set the value template when creating any array, for all examples we will use <code>&lt;(any type)&gt;</code>. This needs to be replaced by any data types, such as values, classes, etc. Examples include <code>&lt;int8&gt;</code>, <code>&lt;boolean&gt;</code>, etc</p> <pre><code>Array&lt;(any type)&gt; ArrayName;\n</code></pre> <p>To create an empty array</p> <pre><code>Array&lt;(any type)&gt; ArrayName = {A, B, C D};\n</code></pre> <p>Create an array with contents already in it</p> <pre><code>Array&lt;(any type)&gt; ArrayName = Array2;\n</code></pre> <p>Create an array based on another array</p>"},{"location":"Engine%20API/Runtime/Core/Containers/Array/#operators","title":"Operators","text":"<pre><code>Array[Integer];\n</code></pre> <p>Returns the item in the index of the array</p> <pre><code>Array = Array2;\n</code></pre> <p>You can overwrite the array to contain another array</p> <pre><code>Array += Array2;\n</code></pre> <p>You can append an array using <code>+=</code> operator</p>"},{"location":"Engine%20API/Runtime/Core/Containers/Array/#functions","title":"Functions","text":"<p>T template</p> <p><code>T</code> will be use as the typename for your value</p> <pre><code>void Init(T Repeat, int Size)\n</code></pre> <p>Will initialize the array by repeating <code>Repeat</code> for the length of <code>Size</code></p> <pre><code>void Init(T Input[], int Size)\n</code></pre> <p>Will initialize the array getting each item in the <code>Input</code> array until we reach <code>Size</code></p> <pre><code>int Length() const\n</code></pre> <p>Returns the length of the array, starting at <code>1</code></p> <pre><code>int Indices() const\n</code></pre> <p>Returns the Indices of the array, similar to <code>Length()</code>, but starts at <code>0</code> instead</p> <pre><code>bool Contains(T Input)\n</code></pre> <p>Returns <code>true</code> if the array contains <code>Input</code></p> <pre><code>bool Find(T Input, int&amp; Value)\n</code></pre> <p>Will try to find first instance of<code>Input</code> in the array and store the index in <code>Value</code>. Returns <code>true</code> if <code>Input</code> exists</p> <pre><code>bool FindLast(T Input, int&amp; Value)\n</code></pre> <p>Similar to <code>Find(T Input, int&amp; Value)</code> except looks for the last instance instead</p> <pre><code>int GetTypeSize()\n</code></pre> <p>Returns the size of value, similar to <code>sizeof(T)</code></p> <pre><code>bool IsValidIndex(int Index)\n</code></pre> <p>Returns <code>true</code> if <code>Index</code> is a valid array slot</p> <pre><code>Array&lt;T&gt; Reverse()\n</code></pre> <p>Returns a copy of an array where all the array slots are reversed <code>(eg [1, 2, 3] = [3, 2, 1])</code></p> <pre><code>T Top()\n</code></pre> <p>Returns the first item in list</p> <pre><code>T Top(int a)\n</code></pre> <p>Returns the item from <code>0</code> to <code>a</code> (<code>0 + a</code>)</p> <pre><code>T Last()\n</code></pre> <p>Returns the last item in list</p> <pre><code>T Last(int a)\n</code></pre> <p>Returns the item from the last to <code>a</code> (<code>[Last Item Index] - a</code>)</p> <pre><code>void Add(T Input)\n</code></pre> <p>Add <code>Input</code> to the array</p> <pre><code>void Append(const Array&amp; Input, int Size)\n</code></pre> <p>Adds <code>Input</code> to the array up to <code>Size</code></p> <pre><code>void AddUnique(T Input)\n</code></pre> <p>Only adds <code>Input</code> to array if <code>Input</code> doesn't exist in the array</p> <pre><code>void Insert(T Input, int Index)\n</code></pre> <p>Insert's <code>Input</code> at <code>Index</code>. Will move the rest of the values to the right</p> <pre><code>void RemoveAt(int Index)\n</code></pre> <p>Removes an item at <code>Index</code></p> <pre><code>T Pop(int Index)\n</code></pre> <p>Removes an item at <code>Index</code>, returns the removed item</p> <pre><code>bool RemoveSingle(T Input)\n</code></pre> <p>Removes only the first instance of <code>Input</code></p> <pre><code>void Remove(T Input)\n</code></pre> <p>Removes all instances of <code>Input</code></p> <pre><code>void Empty()\n</code></pre> <p>Removes everything in the array</p> <pre><code>bool SplitIndex(int Index, Array&lt;T&gt;&amp; Left, Array&lt;T&gt;&amp; Right)\n</code></pre> <p>Split the array into 2 and stored in <code>Left</code> and <code>Right</code> based on <code>Index</code>, <code>Index</code> value will be excluded. Returns true if successful</p> <pre><code>bool SplitIndexInclusive(int Index, Array&lt;T&gt;&amp; Left, Array&lt;T&gt;&amp; Right)\n</code></pre> <p>Similar to <code>bool SplitIndex(int Index, Array&lt;T&gt;&amp; Left, Array&lt;T&gt;&amp; Right)</code> but we will keep the index value and will be stored in <code>Right</code></p> <pre><code>bool Split(const T&amp; ItemToSplit, Array&lt;T&gt;&amp; Left, Array&lt;T&gt;&amp; Right, bool Last = false, bool Inclusive = false)\n</code></pre> <p>Will split the array based on <code>Item</code>, the split will be stored in <code>Left</code> and <code>Right</code>. If <code>Last</code> is <code>true</code>, then we will find the last item instead of the first. If <code>Inclusive</code> is <code>true</code>, then the index value will be included and stored in <code>Right</code>. Returns <code>true</code> if successful</p> <pre><code>void Swap(int A, int B)\n</code></pre> <p>Will swap index <code>A</code> and index <code>B</code>, <code>(example: [1, 2] = [2, 1])</code></p> <pre><code>void Replace(T Input, int Index)\n</code></pre> <p>Will replace item at <code>Index</code> to <code>Input</code></p> <pre><code>T* ReturnPointer()\n</code></pre> <p>Returns the array pointer</p>"},{"location":"Engine%20API/Runtime/Core/Containers/Map/","title":"Map","text":"<p>A map is contains 2 arrays, a key and a value, each value is assigned a key, the key and Value can be seperate value types</p> <p>It can also be included in <code>CoreTypes.h</code> and <code>CoreMinimal.h</code></p>"},{"location":"Engine%20API/Runtime/Core/Containers/Map/#constructor","title":"Constructor","text":"<p>Typename Key and Value</p> <p>This value uses 2 typename, in this doc, they will be represented as <code>TKey</code> for Key typename and <code>TValue</code> for value typename</p> <pre><code>Map&lt;(any type)&gt; MapName;\n</code></pre> <p>Create an empty map</p>"},{"location":"Engine%20API/Runtime/Core/Containers/Map/#operators","title":"Operators","text":"<pre><code>MapName[TKey];\n</code></pre> <p>returns the value from <code>Tkey</code>, if it doesn't exist, it will create a key with default <code>TValue</code> constructor</p>"},{"location":"Engine%20API/Runtime/Core/Containers/Map/#functions","title":"Functions","text":"<pre><code>int Indices()\n</code></pre> <p>Returns the length of map starting at <code>0</code></p> <pre><code>bool Exist(KeyType Name)\n</code></pre> <p>Returns <code>true</code> if the key exists</p> <pre><code>int Find(KeyType A)\n</code></pre> <p>Returns the index of the key, returns <code>-1</code> if it's false</p> <pre><code>KeyType First(int i)\n</code></pre> <p>Returns the key based on index <code>i</code></p> <pre><code>ValueType Second(int i)\n</code></pre> <p>Returns the value based on index <code>i</code></p> <pre><code>void SetAdd(KeyType K, ValueType V)\n</code></pre> <p>If key <code>K</code> exists, replace it's existing value with <code>V</code>, otherwise we will create a new key with the value</p> <pre><code>void Remove(KeyType K)\n</code></pre> <p>Remove both key and value of key <code>K</code></p> <pre><code>Array&lt;KeyType&gt; GetKeys()\n</code></pre> <p>Returns an array of all keys in this Map</p> <pre><code>Array&lt;ValueType&gt; GetValues()\n</code></pre> <p>Returns an array of all values in this Map</p>"},{"location":"Engine%20API/Runtime/Core/Containers/String/","title":"String","text":"<p>The <code>String</code> class is Relight's custom string implementation which stores an <code>array</code> of characters. This class can store words, sentences, or even entire paragraphs.</p> <p>It can also be included in <code>CoreTypes.h</code> and <code>CoreMinimal.h</code></p>"},{"location":"Engine%20API/Runtime/Core/Containers/String/#constructor","title":"Constructor","text":"<pre><code>String StrName;\n</code></pre> <p>To create an empty string</p> <pre><code>String StrName = \"Hello world!\";\n</code></pre> <p>You can add a default string directly when creating the object</p> <pre><code>char* A = {\"a\", \"b\", \"c\"};\nString StrName = A;\n</code></pre> <p>You can also set the default string to be a <code>char</code> pointer</p> <pre><code>UTF16* A = {\"a\", \"b\", \"c\"};\nString StrName = A;\n</code></pre> <p>You can also set the default string to be a UTF16 pointer</p> <pre><code>Array&lt;UTF16&gt; HelloWorld = {\"a\", \"b\", \"c\"};\nString StrName = HelloWorld;\n</code></pre> <p>You can also set the default string using UTF16 Array</p>"},{"location":"Engine%20API/Runtime/Core/Containers/String/#operators","title":"Operators","text":"<pre><code>UTF16 StrName[Integer];\n</code></pre> <p>You can get a specific character using <code>[]</code></p> <pre><code>StrName == StrName2\n</code></pre> <p>returns <code>true</code> if <code>StrName</code> and <code>StrName2</code> is the same, is case sensitive</p> <pre><code>StrName != StrName2\n</code></pre> <p>Return's <code>true</code> if <code>StrName</code> and <code>StrName2</code> are not the same, also case sensitive</p> <pre><code>StrName + StrName2\n</code></pre> <p>Append's <code>StrName2</code> to <code>StrName</code></p>"},{"location":"Engine%20API/Runtime/Core/Containers/String/#functions","title":"Functions","text":"<pre><code>bool Compare(String B, bool CaseSensitive = true)\n</code></pre> <p>Returns <code>true</code> if <code>String</code> and <code>B</code> are equal, if CaseSensitive is <code>false</code>, then capitlization will be ignored</p> <pre><code>char ToChar(int I)\n</code></pre> <p>Convert a character at index <code>I</code> from <code>UTF16</code> to <code>char</code> and returns said <code>char</code></p> <pre><code>int Length() const\n</code></pre> <p>Returns the length of a string, starting from <code>1</code></p> <pre><code>int Indices() const\n</code></pre> <p>Similar to <code>Length()</code> but we start from <code>0</code> instead</p> <pre><code>String ToUpper()\n</code></pre> <p>Returns a new string where all characters are uppercase</p> <pre><code>String ToLower()\n</code></pre> <p>Returns a new string where all characters are lowercase</p> <pre><code>void Append(const UTF16&amp; B)\n</code></pre> <p>Append's character <code>B</code> to String</p> <pre><code>void Append(const String&amp; B)\n</code></pre> <p>Append's String <code>B</code> to String</p> <pre><code>void Append(const UTF16* B)\n</code></pre> <p>Append's UTF16 array <code>B</code> to String</p> <pre><code>void Append(const char* B)\n</code></pre> <p>Append's char array <code>B</code> to String</p> <pre><code>void Add(const UTF16&amp; B)\n</code></pre> <p>The same as <code>void Append(const UTF16&amp; B)</code></p> <pre><code>bool StartsWith(const String&amp; B, bool CaseSensitive = true)\n</code></pre> <p>Returns <code>true</code> if the String starts with <code>B</code>, if <code>CaseSensitive</code> is <code>false</code>, then capitlization will be ignored</p> <pre><code>bool EndsWith(const String&amp; B, bool CaseSensitive = true)\n</code></pre> <p>Returns <code>true</code> if the String ends with <code>B</code>, if <code>CaseSensitive</code> is <code>false</code>, then capitlization will be ignored</p> <pre><code>String Reverse()\n</code></pre> <p>Returns a copy of the string in reverse</p> <pre><code>bool Contains(const UTF16&amp; StrCheck)\n</code></pre> <p>returns <code>true</code> if string contains character <code>StrCheck</code></p> <pre><code>bool Contains(const String&amp; StrCheck)\n</code></pre> <p>returns <code>true</code> if string contains string <code>StrCheck</code></p> <pre><code>int Find(const String&amp; StrCheck, bool CaseSensitive = true)\n</code></pre> <p>finds <code>StrCheck</code> and returns the index of where it's located. Returns <code>-1</code> if it doesn't exist. If <code>CaseSensitive</code> is <code>false</code>, then capitlization will be ignored</p> <pre><code>bool Split(String&amp; Str, String&amp; Left, String&amp; Right, bool CaseSensitive = true)\n</code></pre> <p>finds the index of where <code>Str</code> is located in the string, and split it between <code>Left</code> and <code>Right</code>. If <code>CaseSensitive</code> is <code>false</code>, then capitlization will be ignored. Str can also be <code>const</code></p> <pre><code>void Empty()\n</code></pre> <p>Reset the String and empty it</p> <pre><code>void TrimStart()\n</code></pre> <p>Removes any whitespace from the beginning of the string</p> <pre><code>void TrimEnd()\n</code></pre> <p>Removes any whitespace from the end of the string</p> <pre><code>void Trim()\n</code></pre> <p>Removes any whitespace from both beginning and end of string</p> <pre><code>void TrimStartChar(UTF16 Input)\n</code></pre> <p>Remove the first character if the first character is <code>Input</code></p> <pre><code>void TrimEndChar(UTF16 Input)\n</code></pre> <p>Remove the last character if the last character is <code>Input</code></p> <pre><code>void TrimChar(UTF16 Input)\n</code></pre> <p>Remove the first and last character if the characters are <code>Input</code></p> <pre><code>void TrimQuotes()\n</code></pre> <p>Removes any quotes at beginning and end of string</p> <pre><code>Array&lt;UTF16&gt; ToArr()\n</code></pre> <p>Returns the UTF16 Array</p> <pre><code>void Swap(int A, int B)\n</code></pre> <p>Swap chracters from index <code>A</code> and index <code>B</code></p> <pre><code>void Replace(UTF16 Input, int Index)\n</code></pre> <p>Replaces character and index <code>Index</code> to <code>Input</code></p> <pre><code>Array&lt;char&gt; ToArrayChar()\n</code></pre> <p>Get any valid characters and return a char Array with them</p>"},{"location":"Engine%20API/Runtime/Core/Etc/CharUtil/","title":"CharUtil","text":"<p>CharUtil.h contains utilities for handling characters (both <code>char</code> and <code>UTF16</code>)</p>"},{"location":"Engine%20API/Runtime/Core/Etc/CharUtil/#functions","title":"Functions","text":"<pre><code>static int CharUtil::CharToInt(char Character)\n</code></pre> <p>Convert a char to an integer</p> <pre><code>static char CharUtil::IntToChar(int DEC)\n</code></pre> <p>Convert an integer into a character</p> <pre><code>static char CharUtil::ToUpper(char Character)\n</code></pre> <p>Convert character to uppercase</p> <pre><code>static char CharUtil::ToLower(char Character)\n</code></pre> <p>Convert the ASCII character into lowercase</p> <pre><code>static bool CharUtil::IsUpper(CharType Character)\n</code></pre> <p>Returns <code>true</code> if a character is a capital number. <code>CharType</code> can be any valid character type</p> <pre><code>static bool CharUtil::IsLower(CharType Character)\n</code></pre> <p>Returns <code>true</code> if a character is a lowercase number. <code>CharType</code> can be any valid character type</p> <pre><code>static bool CharUtil::IsASCII(CharType Character)\n</code></pre> <p>Returns <code>true</code> if a character is ASCII compatible. <code>CharType</code> can be any valid character type</p>"},{"location":"Engine%20API/Runtime/Core/Etc/Config%20Runtime/","title":"Config Runtime","text":""},{"location":"Engine%20API/Runtime/Core/Etc/Config%20Runtime/#config-runtime-api","title":"Config (Runtime API)","text":"<p>Note</p> <p>This section refers to API calls that interact with config files, if you wanna learn how to write and modify config files, then it's located at Dealing with content</p> <p>Config.h controls adding configs files and getting values from configs</p>"},{"location":"Engine%20API/Runtime/Core/Etc/Config%20Runtime/#functions","title":"functions","text":"<pre><code>static void Config::AddFile(String LocalFilePath, String ForceFullPath = \"\")\n</code></pre> <p>Adds a config file and stores it's contents. <code>LocalFilePath</code> gets the file from the config and saved directory. <code>ForceFullPath</code> will override the full file path (this is recommended only for specific needs)</p> <pre><code>static bool Config::GetString(String Section, String Variable, String&amp; Output, String File)\n</code></pre> <p>Gets a config value and store it as a String, returns <code>true</code> if successful. <code>Section</code> will get the sepcified section the value is located at, <code>Variable</code> will be the name of the value we are getting, <code>Output</code> is where we will store the value, and <code>File</code> is the specified file the value is located in</p> <pre><code>static bool Config::GetInt(String Section, String Variable, int&amp; Output, String File)\n</code></pre> <p>Gets a config value and store it as an 32-bit integer, returns <code>true</code> if successful. <code>Section</code> will get the sepcified section the value is located at, <code>Variable</code> will be the name of the value we are getting, <code>Output</code> is where we will store the value, and <code>File</code> is the specified file the value is located in</p> <pre><code>static bool Config::GetDouble(String Section, String Variable, double&amp; Output, String File)\n</code></pre> <p>Gets a config value and store it as a double, returns <code>true</code> if successful. <code>Section</code> will get the sepcified section the value is located at, <code>Variable</code> will be the name of the value we are getting, <code>Output</code> is where we will store the value, and <code>File</code> is the specified file the value is located in</p> <pre><code>static bool Config::GetFloat(String Section, String Variable, float&amp; Output, String File)\n</code></pre> <p>Gets a config value and store it as a float, returns <code>true</code> if successful. <code>Section</code> will get the sepcified section the value is located at, <code>Variable</code> will be the name of the value we are getting, <code>Output</code> is where we will store the value, and <code>File</code> is the specified file the value is located in</p> <pre><code>static bool Config::GetString(String Section, String Variable, bool&amp; Output, String File)\n</code></pre> <p>Gets a config value and store it as a boolean, returns <code>true</code> if successful. <code>Section</code> will get the sepcified section the value is located at, <code>Variable</code> will be the name of the value we are getting, <code>Output</code> is where we will store the value, and <code>File</code> is the specified file the value is located in</p> <pre><code>static bool Config::GetArray(String Section, String Variable, Array&lt;String&gt;&amp; Output, String File)\n</code></pre> <p>Gets a config value and store it as a string array, returns <code>true</code> if successful. <code>Section</code> will get the sepcified section the value is located at, <code>Variable</code> will be the name of the value we are getting, <code>Output</code> is where we will store the value, and <code>File</code> is the specified file the value is located in. Note that the array will get the same value from the same section, and some can be added or removed depending on what sections you run</p>"},{"location":"Engine%20API/Runtime/Core/Etc/FileManager/","title":"FileManager","text":"<p><code>FileManager</code> is a custom static class that allows the engine to preform file operations. It uses the Platform class as a base for handling files</p>"},{"location":"Engine%20API/Runtime/Core/Etc/FileManager/#functions","title":"Functions","text":"<p><pre><code>static bool FileManager::FileExists(String File)\n</code></pre> Will return <code>true</code> if <code>File</code> exists</p> <p><pre><code>static bool FileManager::ReadFile(String File, FileReader&amp; Output)\n</code></pre> Will read a <code>File</code> and store it's contents in the FileReader Archive called <code>Output</code>. Return true if we are successful.</p> <p><pre><code>static bool FileManager::WriteFile(String File, Array&lt;int&gt; Contents)\n</code></pre> Will write <code>Contents</code> to <code>File</code>, return's <code>true</code> if successful</p> <p><pre><code>static bool FileManager::WriteFile(String File, int* Contents, int Size)\n</code></pre> Will write the integer array of <code>Contents</code> to <code>File</code>, return's <code>true</code> if successful</p> <p><pre><code>static bool FileManager::WriteFile(String File, String Contents)\n</code></pre> Will write string <code>Contents</code> to <code>File</code>, return's <code>true</code> if successful</p> <p><pre><code>static bool FileManager::CopyFile(String A, String B)\n</code></pre> Will copy file <code>A</code> to file <code>B</code>, return <code>true</code> if successful</p> <p><pre><code>static bool FileManager::DeleteFile(String File)\n</code></pre> Will delete <code>File</code>, return <code>true</code> if successful</p> <p><pre><code>static bool FileManager::MoveFile(String File, String Directory)\n</code></pre> Move <code>File</code> to <code>Directory</code>, return <code>true</code> if successful</p> <p><pre><code>static bool FileManager::DirectoryExists(String Directory)\n</code></pre> Will return <code>true</code> if <code>Directory</code> exists</p> <p><pre><code>static bool FileManager::CreateDirectory(String Directory)\n</code></pre> Will create <code>Directory</code>, return <code>true</code> if successful</p> <p><pre><code>static bool FileManager::ListFiles(String Directory, Array&lt;String&gt;&amp; Files, Array&lt;String&gt;&amp; Directories, String Ext = \"NULL\")\n</code></pre> Will find all files and directories from <code>Directory</code>, and store all files in <code>Files</code> and Store all Directories in <code>Directories</code>, if <code>Ext</code> is not <code>NULL</code>, then we will ignore files with those extensions (examples: <code>png</code>, <code>jpg</code>, <code>mp4</code>). Return's true if successful</p> <p><pre><code>static bool FileManager::ListFilesRecersive(String Directory, Array&lt;String&gt;&amp; Files, Array&lt;String&gt;&amp; Directories, String Ext = \"NULL\")\n</code></pre> Similar to <code>FileManager::ListFiles</code>, but will recersively run for each subdirectory</p>"},{"location":"Engine%20API/Runtime/Core/Etc/FromString/","title":"FromString","text":"<p>FromString is a static struct that handles converting a string into different value types</p>"},{"location":"Engine%20API/Runtime/Core/Etc/FromString/#functions","title":"Functions","text":"<p><pre><code>static int FromString::Int(String&amp; Input)\n</code></pre> Converts <code>Input</code> to int and returns it</p> <p><pre><code>static double FromString::Double(String&amp; Input)\n</code></pre> Converts <code>Input</code> to double and returns it</p> <p><pre><code>static float FromString::Float(String&amp; Input)\n</code></pre> Converts <code>Input</code> to float and returns it</p> <p><pre><code>static bool FromString::Bool(String&amp; Input)\n</code></pre> Converts <code>Input</code> to boolean and returns it</p>"},{"location":"Engine%20API/Runtime/Core/Etc/Path/","title":"Path","text":"<p><code>Path</code> contains functions of getting common project directories. This acts as an interface to direct platform functions. All functions will use <code>absolute paths</code>, meaning the full directory, including <code>C:\\</code> on windows and <code>/</code> on Unix-based systems, is included in the output.</p>"},{"location":"Engine%20API/Runtime/Core/Etc/Path/#functions","title":"Functions","text":"<p><pre><code>static bool Path::ExeDir(String&amp; Output)\n</code></pre> Store the game's directory into Output, return's <code>true</code> if successful</p> <p>An example of this would be (<code>Game/</code>)</p> <p><pre><code>static bool Path::ConfigDir(String&amp; Output)\n</code></pre> Store the game's config directory into Output, return's <code>true</code> if successful</p> <p>An example of this would be (<code>Game/Config</code>)</p> <p><pre><code>static bool Path::ContentDir(String&amp; Output)\n</code></pre> Store the game's content directory into Output, return's <code>true</code> if successful</p> <p>An example of this would be (<code>Game/Content</code>)</p> <p><pre><code>static bool Path::ShadertDir(String&amp; Output)\n</code></pre> Store the game's shader directory into Output, return's <code>true</code> if successful</p> <p>An example of this would be (<code>Game/Shader</code>)</p> <p><pre><code>static bool Path::SavedDir(String&amp; Output)\n</code></pre> Store the game's Save directory into Output, return's <code>true</code> if successful</p> <p>An example of this would be (<code>Game/Saved</code>)</p> <p><pre><code>static bool Path::SavedConfigDir(String&amp; Output)\n</code></pre> Store the game's Save configuration directory into Output, return's <code>true</code> if successful</p> <p>An example of this would be (<code>Game/Saved/Config</code>)</p> <p><pre><code>static bool Path::EngineDir(String&amp; Output)\n</code></pre> Store the game's engine directory into Output, return's <code>true</code> if successful</p> <p>An example of this would be (<code>Game/Engine</code>)</p>"},{"location":"Engine%20API/Runtime/Core/Etc/ToString/","title":"ToString","text":"<p>ToString holds functions that will convert a variable to string</p>"},{"location":"Engine%20API/Runtime/Core/Etc/ToString/#functions","title":"Functions","text":"<p><pre><code>inline String CORE_API::ToString(int Input)\n</code></pre> Return's a string based on int</p> <p><pre><code>inline String CORE_API::ToString(float Input)\n</code></pre> Return's a string based on float</p> <p><pre><code>inline String CORE_API::ToString(double Input)\n</code></pre> Return's a string based on double</p> <p><pre><code>inline String CORE_API::ToString(bool Input)\n</code></pre> Return's a string based on boolean</p> <p><pre><code>inline String CORE_API::ToString(String Input)\n</code></pre> Return's a string, simply here to aid in Array conversion</p> <p><pre><code>inline String CORE_API::ToString(const UTF16* Input)\n</code></pre> Return's a string based on UTF16 pointer array</p> <p><pre><code>inline String CORE_API::ToString(const char (&amp;Input)[N])\n</code></pre> Return's a string based on char pointer array</p> <p><pre><code>inline String CORE_API::ToString(Array&lt;ArrayInput&gt; Input)\n</code></pre> Return's a string based on an array (note: <code>ArrayInput</code> is a template for any type)</p>"},{"location":"Engine%20API/Runtime/Core/Hardware/RMemory/","title":"RMemory","text":"<p>Placeholder functions</p> <p>custom memory functions do not exist yet, for now it uses system defaults, this is planned to change in the future</p> <p>RMemory is relight's custom memory management struct. Handling allocating and deallocating memory</p>"},{"location":"Engine%20API/Runtime/Core/Hardware/RMemory/#functions","title":"Functions","text":"<p><pre><code>static void* RMemory::Malloc(size_t Size, int Alignment = 0)\n</code></pre> Allocate's memory on heap for <code>Size</code> and return it's pointer</p> <p><pre><code>static void* RMemory::Realloc(void* Ptr, size_t Size, int Alignment = 0)\n</code></pre> changes size to <code>Size</code> for <code>Ptr</code>, may expand and/or move to new location, return new pointer</p> <p><pre><code>static void RMemory::Free(void* Ptr)\n</code></pre> Free's <code>Ptr</code> from memory</p> <p><pre><code>static void* RMemory::Memmove(void* Dest, void* Src, size_t Size)\n</code></pre> Copy's the source bytes to destination. <code>Dest</code> determines the destination, <code>Src</code> determines the source, and <code>Size</code> determines the amount of bytes to copy. Returns destination pointer. This method is a bit slower than <code>Memcpy</code> but is overlap safe, which is useful if you are unsure if the memory block will overlap</p> <p><pre><code>static int RMemory::Memcmp(void* A, void* B, size_t Size)\n</code></pre> Compares memory <code>A</code> and memory <code>B</code> for <code>Size</code> amount of bytes to check, returns <code>0</code> if the 2 blocks are equal, <code>positive number</code> if <code>A</code> is bigger, or <code>negative number</code> if <code>B</code> is bigger</p> <p><pre><code>static void* RMemory::Memset(void* A, int C, size_t Size)\n</code></pre> Fills the memory block <code>A</code> with the content <code>C</code> for <code>Size</code> bytes. Returns filled pointer</p> <p><pre><code>static void* RMemory::Memcpy(void* Dest, void* Src, size_t Size)\n</code></pre> Similar to to <code>Memmove</code>, but with the main difference being that <code>memcpy</code> isn't safe with overlaps (will cause undefined behavior), but it is faster than <code>Memmove</code>. Only use this if you are 100% sure that the new memory region does not overlap</p>"},{"location":"Engine%20API/Runtime/Core/Log/Log/","title":"Log","text":"<p>Log is how we Relight Engine store information about the runtime to the user. It uses Relight's custom logging system, and will display messages, warnings, errors, and any potential fatal errors the program encounters.</p> <p>Log is also defined in <code>CoreMinimal.h</code></p>"},{"location":"Engine%20API/Runtime/Core/Log/Log/#using-log","title":"Using LOG","text":"<p>To use log, you use this function</p> <p><pre><code>LOG(CORE_API::LogCategory Category, LogType LogWarning, String Text)\n</code></pre> Category - The category this log belongs in</p> <p>LogWarning - The type of warning this log will use</p> <p>Text - The message to display</p> <p><pre><code>LOG(String Text)\n</code></pre> If you only have the string defined, then we will use <code>LogCore</code> as the category and <code>LogType::Log</code> as the Log Type</p>"},{"location":"Engine%20API/Runtime/Core/Log/Log/#log-type","title":"Log type","text":"<p>The log type will determine how the message will be displayed, and how the game will respond to the log. Here's the list of all the log types:</p> <p><code>LogType::Log</code> - A basic level of logging, this let the developer know basic information on what the engine/project is doing. It doesn't not warrent any intention unless needed</p> <p><code>LogType::Warning</code> - Contains any issues detected by the Engine, but nothing that warrents any immediate concern</p> <p><code>LogType::Error</code> - Contains any issues detected by the engine that should get immediate attention, but the runtime will continue</p> <p><code>LogType::Fatal</code> - Contains any issues detected by the engine and will cause the runtime to shutdown.</p>"},{"location":"Engine%20API/Runtime/Core/Log/Log/#log-category","title":"Log Category","text":"<p>To create your own category, you run this function</p> <p><pre><code>CORE_API::LogCategory LogExample(String Name);\n</code></pre> This is an example of creating a log category. <code>LogExample</code> will be the name you put in the <code>LOG</code> function, <code>Name</code> will be what is displayed in the Log system. It is recommended to have both <code>LogExample</code> and <code>Name</code> to be the same for simplicity sake</p>"},{"location":"Engine%20API/Runtime/Core/Math/Range/","title":"Range","text":"<p><code>Range</code> is a class that is very similar to arrays, however is optimized for huge sets of numbers. How it works is that instead of storing each individual number, we will only hold a min value and a max value, and any number in-between these 2 numbers will be consider part of the 'set'</p>"},{"location":"Engine%20API/Runtime/Core/Math/Range/#constructor","title":"Constructor","text":"<p>We will use <code>T</code> as a placeholder for a typename</p> <p><pre><code>CORE_API::Math::Range&lt;T&gt; ValueName(T Both)\n</code></pre> Create a range of <code>[Both, Both]</code> with both values being inclusive</p> <p><pre><code>CORE_API::Math::Range&lt;T&gt; ValueName(T Lower, T Higher)\n</code></pre> Create a range of <code>[Lower, Higher)</code> with Lower value being inclusive and Higher value being exclusive</p> <p><pre><code>CORE_API::Math::Range&lt;T&gt; ValueName(RangeBound&lt;T&gt; Lower, RnageBound&lt;T&gt; Higher)\n</code></pre> Set the range based on predefined bounds using <code>RangeBound</code></p>"},{"location":"Engine%20API/Runtime/Core/Math/Range/#functions","title":"Functions","text":"<p><pre><code>bool IsAdjoint(Range&amp; Second)\n</code></pre> returns <code>true</code> if the parent range and <code>Second</code> are directly next to each other (not overlapping and no gaps)</p> <p><pre><code>bool Contains(T Value)\n</code></pre> returns <code>true</code> if Value is contained in range</p>"},{"location":"Engine%20API/Runtime/Core/Math/RangeBound/","title":"RangeBound","text":"<p>RangeBound acts as a class implementation of a boundary of a range, whether that would be the beginning or the end, it is created primary for Range class</p>"},{"location":"Engine%20API/Runtime/Core/Math/RangeBound/#rangeboundtype","title":"RangeBoundType","text":"<p>RangeBoundType is an enum class that declares what type of bound we will use</p> <p><code>CORE_API::Math::RangeBoundType::Inclusive</code> - the bound is Inclusive, meaning we will include the bound value (example: if bound value is <code>5</code>, then we will include LowerBound-5. Denoted with <code>[]</code>)</p> <p><code>CORE_API::Math::RangeBoundType::Exclusive</code> - the bound is Exclusive, meaning we will not include the bound value (example: if bound value is <code>5</code>, then we will include LowerBound-4. Denoted with <code>()</code>)</p> <p><code>CORE_API::Math::RangeBoundType::Open</code> - the bound is open, meaning it goes on for infinity (example: LowerBound-Infinity)</p>"},{"location":"Engine%20API/Runtime/Core/Math/RangeBound/#constructor","title":"Constructor","text":"<p>We will use <code>T</code> as a placeholder for a typename</p> <p><pre><code>CORE_API::Math::RangeBound&lt;T&gt; ValueName;\n</code></pre> Create and set RangeBound to open</p> <p><pre><code>CORE_API::Math::RangeBound&lt;T&gt; ValueName(T Value);\n</code></pre> Create and set RangeBound an inclusive <code>Value</code></p>"},{"location":"Engine%20API/Runtime/Core/Math/RangeBound/#functions","title":"functions","text":"<p><pre><code>bool IsNotOpen()\n</code></pre> return's <code>true</code> if bound is not open</p> <p><pre><code>T ReturnNotOpen()\n</code></pre> return's the value if the bound type is not open</p> <p><pre><code>T SetNotOpen(T Value)\n</code></pre> Set the bound to <code>Value</code> if bound is not open</p> <p><pre><code>bool IsInclusive()\n</code></pre> return's <code>true</code> if bound is Inclusive</p> <p><pre><code>bool IsExclusive()\n</code></pre> return's <code>true</code> if bound is Exclusive</p> <p><pre><code>bool IsOpen()\n</code></pre> return's <code>true</code> if bound is Open</p>"},{"location":"Engine%20API/Runtime/Core/Math/RangeBound/#static-functions","title":"static functions","text":"<p>Redo this</p> <p>all static functions here are incorrect, please fix it, once fix, document the code here</p>"},{"location":"Engine%20API/Runtime/Core/Math/Vector/","title":"Vector","text":"<p>A vector stores 3 values, <code>X</code>, <code>Y</code>, and <code>Z</code>. these values can be an integer or any type of float</p>"},{"location":"Engine%20API/Runtime/Core/Math/Vector/#constructor","title":"Constructor","text":"<p><code>FloatType</code> will be used as a placeholder for typename (note: integers can also be accepted)</p> <p><pre><code>CORE_API::Math::Vector&lt;FloatType&gt; MyValue;\n</code></pre> Set all 3 values to <code>0</code></p> <p><pre><code>CORE_API::Math::Vector&lt;FloatType&gt; MyValue(FloatType InX, FloatType InY, FloatType InZ);\n</code></pre> Set X to <code>InX</code>, Y to <code>InY</code>, and Z to <code>InZ</code></p> <p><pre><code>CORE_API::Math::Vector&lt;FloatType&gt; MyValue(FloatType All);\n</code></pre> Set X, Y, and Z to <code>All</code></p>"},{"location":"Engine%20API/Runtime/Core/Math/Vector/#functions","title":"Functions","text":"<p><pre><code>String ToString()\n</code></pre> returns the vector as a full string, useful for debugging</p> <p><pre><code>FloatType GetX()\n</code></pre> returns the X value</p> <p><pre><code>FloatType GetY()\n</code></pre> returns the Y value</p> <p><pre><code>FloatType GetZ()\n</code></pre> returns the Z value</p> <p><pre><code>FloatType SetX(FloatType Input)\n</code></pre> Set the X value to <code>Input</code></p> <p><pre><code>FloatType SetY(FloatType Input)\n</code></pre> Set the Y value to <code>Input</code></p> <p><pre><code>FloatType SetZ(FloatType Input)\n</code></pre> Set the Z value to <code>Input</code></p> <p><pre><code>FloatType Set(FloatType InX, FloatType InY, FloatType InZ)\n</code></pre> Set X to <code>InX</code>, Y to <code>InY</code>, and Z to <code>InZ</code></p> <p><pre><code>bool Serialize(Archive&amp; Container)\n</code></pre> Serialize the vector to <code>Container</code>, returns <code>true</code> if successful</p>"},{"location":"Engine%20API/Runtime/Core/Platform/PlatformFile/","title":"PlatformFile","text":"<p>PlatformFile handles an implementation of handling files and folders of an operating system.</p> <p>All functions will return <code>true</code> when successful</p>"},{"location":"Engine%20API/Runtime/Core/Platform/PlatformFile/#functions","title":"functions","text":"<p><pre><code>static bool PlatformFile::FileExists(String File)\n</code></pre> Checks if <code>File</code> exists</p> <p><pre><code>static bool PlatformFile::ReadFile(String File, char** Output, size_t* OutputSize)\n</code></pre> Read's a <code>File</code> and store it in <code>Output</code> and store the size of the file in <code>OutputSize</code></p> <p><pre><code>static bool PlatformFile::CreateFile(String File)\n</code></pre> Create's a file called <code>File</code></p> <p><pre><code>static bool PlatformFile::WriteFile(String File, Array&lt;int&gt; Contents)\n</code></pre> Write's a file called <code>File</code> and writes <code>Contents</code> to the file</p> <p><pre><code>static bool PlatformFile::CopyFile(String FileA, String FileB)\n</code></pre> Copies <code>FileA</code> and put the copy at <code>FileB</code></p> <p><pre><code>static bool PlatformFile::DeleteFile(String File)\n</code></pre> Delete's the file <code>File</code></p> <p><pre><code>static bool PlatformFile::MoveFile(String File, String MoveDirectory)\n</code></pre> Get's the file <code>File</code> and moves it to <code>MoveDirectory</code></p> <p><pre><code>static bool PlatformFile::DirectoryExists(String Directory)\n</code></pre> returns <code>true</code> if directory <code>Directory</code> exists</p> <p><pre><code>static bool PlatformFile::CreateDirectory(String Directory)\n</code></pre> Create's a directory called <code>Directory</code></p> <p><pre><code>static bool PlatformFile::ListFiles(String Directory, Array&lt;String&gt;&amp; Output, Array&lt;String&gt;&amp; Directories, String Ext = \"NULL\")\n</code></pre> Will find all files and directories from <code>Directory</code>, and store all files in <code>Output</code> and Store all Directories in <code>Directories</code>, if <code>Ext</code> is not <code>NULL</code>, then we will ignore files with those extensions (examples: <code>png</code>, <code>jpg</code>, <code>mp4</code>). Return's true if successful</p> <p><pre><code>static const char* PlatformFile::ToConstChar(String Input)\n</code></pre> Convert's string <code>Input</code> to <code>const char*</code> and returns it</p>"},{"location":"Engine%20API/Runtime/Core/Platform/PlatformMisc/","title":"PlatformMisc","text":"<p>PlatformMisc stores additional functions that the OS could use</p>"},{"location":"Engine%20API/Runtime/Core/Platform/PlatformMisc/#functions","title":"Functions","text":"<p><pre><code>static bool PlatformMisc::ExeDir(String&amp; Output)\n</code></pre> Store the game's directory into Output, return's <code>true</code> if successful</p> <p>An example of this would be (<code>Game/</code>)</p> <p><pre><code>static bool PlatformMisc::ConfigDir(String&amp; Output)\n</code></pre> Store the game's config directory into Output, return's <code>true</code> if successful</p> <p>An example of this would be (<code>Game/Config</code>)</p> <p><pre><code>static bool PlatformMisc::ContentDir(String&amp; Output)\n</code></pre> Store the game's content directory into Output, return's <code>true</code> if successful</p> <p>An example of this would be (<code>Game/Content</code>)</p> <p><pre><code>static bool PlatformMisc::ShadertDir(String&amp; Output)\n</code></pre> Store the game's shader directory into Output, return's <code>true</code> if successful</p> <p>An example of this would be (<code>Game/Shader</code>)</p> <p><pre><code>static bool PlatformMisc::SavedDir(String&amp; Output)\n</code></pre> Store the game's Save directory into Output, return's <code>true</code> if successful</p> <p>An example of this would be (<code>Game/Saved</code>)</p> <p><pre><code>static bool PlatformMisc::SavedConfigDir(String&amp; Output)\n</code></pre> Store the game's Save configuration directory into Output, return's <code>true</code> if successful</p> <p>An example of this would be (<code>Game/Saved/Config</code>)</p> <p><pre><code>static bool PlatformMisc::EngineDir(String&amp; Output)\n</code></pre> Store the game's engine directory into Output, return's <code>true</code> if successful</p> <p>An example of this would be (<code>Game/Engine</code>)</p>"},{"location":"Engine%20API/Runtime/Core/Platform/PlatformOutput/","title":"PlatformOutput","text":"<p>PlatformOutput controls the output for an operating systems, these usually handle the 'print' operator</p>"},{"location":"Engine%20API/Runtime/Core/Platform/PlatformOutput/#functions","title":"functions","text":"<p><pre><code>static void PlatformOutput::Print(const String&amp; Input)\n</code></pre> Print's out string <code>Input</code> to system's terminal output</p> <p><pre><code>static void PlatformOutput::Print(const Array&lt;UTF16&gt;&amp; Input)\n</code></pre> Print's out array <code>Input</code> to system's terminal output</p> <p><pre><code>static void PlatformOutput::Print(const UTF16&amp; Input)\n</code></pre> Print's out UTF-16 character <code>Input</code> to system's terminal output</p> <p><pre><code>static void PlatformOutput::Print(const char* Input)\n</code></pre> Print's out char pointer <code>Input</code> to system's terminal output</p> <p><pre><code>static void PlatformOutput::Print(const NonStringType&amp; Input)\n</code></pre> Convert the non-string <code>Input</code> into a string, then Print's out the string to system's terminal output. <code>NonStringType</code> is a template representing any non-string</p> <p><pre><code>static void PlatformOutput::Println(const StringType&amp; Input)\n</code></pre> Print's out string <code>Input</code> and add's a new line to system's terminal output. StringType represents any string/non-string that <code>Print()</code> supports</p>"},{"location":"Engine%20API/Runtime/Core/Serialization/Archive/","title":"Archive","text":"<p>Archive acts as a base and a wrapper for all serialization types. Serialization is the process of converting an object state or data strucutre into a storable format. Examples include Memory Reader and Writer, a File Reader and Writer, etc. All serialization systems uses this class as a base</p>"},{"location":"Engine%20API/Runtime/Core/Serialization/Archive/#constructor","title":"Constructor","text":"<p><pre><code>Archive ArchiveClass;\n</code></pre> Create an archive</p>"},{"location":"Engine%20API/Runtime/Core/Serialization/Archive/#functions","title":"Functions","text":"<p><pre><code>virtual void Serialize(void* Input, int Length)\n</code></pre> Serialize the data <code>Input</code> for the size of <code>Length</code>. By default, Archive will just run <code>Memset</code> on the data</p> <p><pre><code>virtual void Flush()\n</code></pre> Forces data to stop and push the data into final destination. By default, it does nothing</p> <p><pre><code>virtual void Seek(int Offset)\n</code></pre> Set the current offset into backing data storage by position <code>Offset</code>. By default, it does nothing</p> <p><pre><code>bool DoesReturnCode()\n</code></pre> Return's <code>true</code> if the archive contains code</p> <p><pre><code>bool DoesContainRMap()\n</code></pre> Return's <code>true</code> if the archive contains <code>RMap</code> data</p> <p><pre><code>bool DoesIsSaveLoadGame()\n</code></pre> Return's <code>true</code> if the archive is for saving and/or loading a game</p> <p><pre><code>bool DoesIsCountingMem()\n</code></pre> Return's <code>true</code> if the archive count memory</p> <p><pre><code>bool DoesIsLoading()\n</code></pre> Return's <code>true</code> if the archive is for loading data</p> <p><pre><code>bool DoesIsSaving()\n</code></pre> Return's <code>true</code> if the archive is for saving data</p> <p><pre><code>bool DoesIsTracking()\n</code></pre> Return's <code>true</code> if the archive tracks content (like undo/redo actions)</p> <p><pre><code>bool DoesIsLoadingPak()\n</code></pre> Return's <code>true</code> if the archive is loading from a pak file</p> <p><pre><code>bool DoesIsError()\n</code></pre> Return's <code>true</code> if the archive contains errors</p> <p><pre><code>bool DoesIsFatal()\n</code></pre> Return's <code>true</code> if the archive contains fatal errors</p>"},{"location":"Engine%20API/Runtime/Core/Serialization/ArchiveProxy/","title":"ArchiveProxy","text":"<p>ArchiveProxy is a custom Archive that wraps around another archive, it allows overrides and modifications of the parent archive without changing it directly</p>"},{"location":"Engine%20API/Runtime/Core/Serialization/ArchiveProxy/#constructors","title":"Constructors","text":"<p><pre><code>ArchiveProxy ArchiveProxyClass(Archive&amp; InArchive);\n</code></pre> create an proxy archive based on <code>InArchive</code></p>"},{"location":"Engine%20API/Runtime/Core/Serialization/ArchiveProxy/#functions","title":"Functions","text":"<p><pre><code>virtual void Serialize(void* Input, int Length)\n</code></pre> Serialize the data <code>Input</code> for the size of <code>Length</code>. By default, It will run parent Serialization</p> <p><pre><code>virtual void Flush()\n</code></pre> Forces data to stop and push the data into final destination. By default, it will run parent Flush</p> <p><pre><code>virtual void Seek(int Offset)\n</code></pre> Set the current offset into backing data storage by position <code>Offset</code>. By default, It will run parent Seek</p>"},{"location":"Engine%20API/Runtime/Core/Serialization/FileReader/","title":"FileReader","text":"<p>Rewrite planned</p> <p>This class is planned to get rewritten, all these functions are temporarily</p> <p>FileReader is a container that stores data from a file that's been read</p>"},{"location":"Engine%20API/Runtime/Core/Serialization/FileReader/#operators","title":"Operators","text":"<p><pre><code>FileReaderClass[Integer]\n</code></pre> Get's a constant character from file contents</p>"},{"location":"Engine%20API/Runtime/Core/Serialization/FileReader/#functions","title":"Functions","text":"<p><pre><code>void Serialize(void* Input, int Length) override\n</code></pre> Stores <code>Input</code>, which contains file contents, and set the size to <code>Length</code></p> <p><pre><code>size_t GetSize()\n</code></pre> Return's the size of the file contents</p> <p><pre><code>String ToString\n</code></pre> convert file contents to string</p>"},{"location":"Tools%20and%20Programs/","title":"Tools and Programs","text":"<p>Relight Engine uses external tools for developing and publishing Relight Applications. All external code is located in /Programs</p> <p>RelightBuildTool - Program written in python that compiles the project and engine</p>"},{"location":"Tools%20and%20Programs/RelightBuildTool/","title":"RelightBuildTool","text":"<p>RelightBuildTool is a cross-platform, C/C++ compiler abstractor written in python.</p> <p>Running RelightBuildTool - Learn how to Run RelightBuildTool on your project, also teaches library requirements, command line parameters, and how to compile the engine itself</p> <p>Target Files - Learn how to make target files and all options related to it</p> <p>Module Files - Learn how to make Module files and all options related to it</p> <p>Using a Custom SDK - Learn how to create a custom SDK to port the engine to your targeted system</p> <p>Other Modes - Learn other modes RBT offers, such as cleaning intermediate files and generating project files</p> <p>Behind the Scenes - Learn how RBT compiles a program in the background</p>"},{"location":"Tools%20and%20Programs/RelightBuildTool/Module%20Files/","title":"Module Files","text":"<p>malicious code risk</p> <p>Module files can contain code that could harm your computer, since it uses python as a backend. Please check and verify any module files you see online</p> <p>Module files (<code>.Module</code>) are files that acts as a configuration for a specific module in your project/engine.</p>"},{"location":"Tools%20and%20Programs/RelightBuildTool/Module%20Files/#syntax","title":"Syntax","text":"<p>just like Target files, Module files internally uses python, meaning all syntax and libraries are the exact same as python</p>"},{"location":"Tools%20and%20Programs/RelightBuildTool/Module%20Files/#read-only-configs","title":"Read-only configs","text":"<p>The following are settings that cannot be set by the module file, but can be read, allowing for more personalized settings</p> <p><code>TARGET_NAME</code> - A string that contains the name of the target</p> <p><code>TARGET_DIRECTORY</code> - A String that contains the directory of the target</p> <p><code>PROJECT</code> - A String that contains the full project name and directory</p> <p><code>PLATFORM</code> - A String that contains the platform we are compiling on</p> <p><code>PLATFORM_GROUP</code> - A String that contains the platform group we are compiling on</p> <p><code>FILEPATH</code> - A String that contains the full file path of the target file</p> <p><code>FilePath</code> - A string that contains the full file path and file name of a module (Directory and file name)</p> <p><code>ModuleDirectory</code> - A String that contains the full file path of a module (Just the directory)</p>"},{"location":"Tools%20and%20Programs/RelightBuildTool/Module%20Files/#module-configs","title":"Module Configs","text":"<p>Configuration that will control the specific module. The following is all the options you can set</p>"},{"location":"Tools%20and%20Programs/RelightBuildTool/Module%20Files/#global-configs","title":"Global Configs","text":"<p><code>Name = {String}</code> - The name of the module</p> <p><code>ObjectName = {String}</code> - The name of the object, mainly should be used as a replacement for long module names</p> <p><code>Description = {String}</code> - The description of the module, mainly used for documentation and debugging</p> <p><code>Type = {String}</code> - The type of module. <code>Internal</code> is a Relight-based project, while <code>External</code> is a 3rd-party project. Default is <code>Internal</code></p> <p><code>Modules = {String List}</code> - The list of dependencies this module requires</p> <p><code>ModulesIncludes = {String List}</code> - Names of Modules we will include</p> <p><code>Includes = {String List}</code> - All directories we will include</p> <p><code>SysIncludes = {String List}</code> - includes that act like system (might be used by some 3rd party)</p> <p><code>Defines = {String List}</code> - Defines for this specific module</p> <p><code>DynamicModulePaths = {String List}</code> - Directories to search for dynamic libraries</p> <p><code>Static3rdParty = {String List}</code> - Static libraries for 3rd parties (.a/.lib)</p> <p><code>ThirdParty = {String List}</code> - Third party software this module will use, will be added to Static3rdParty and Includes</p> <p><code>Optimization = {String}</code> - What level of optimization we should use, less optimization = faster compiling and debugging, but bigger file size</p> <p><code>DisableUnity = {Boolean}</code> - If true, we will not use UNITY system on this module, even if the target/project file allows UNITY. Default is false</p> <p><code>RTTI = {Boolean}</code> - if we should use RTTI (run time type information). Default is false</p> <p><code>AVX = {Boolean}</code> - if we should use AVX instructions. Default is false</p> <p><code>AutoIncludeModules = {Boolean}</code> - If true, then we will automatically append Includes to have Modules, you will have to manually do it if false. Default is true</p> <p><code>IsEngineModule = {Boolean}</code> - If true, we will treat it as a engine module (put module in engine dir instead of project dir). Default is false</p> <p><code>ModuleUnityMinSourceFiles = {Integer}</code> - The amount of source files needed to use Unity files, will override Target. Leave number negative to disable override, requires Unity to be true in target. Default is -1</p>"},{"location":"Tools%20and%20Programs/RelightBuildTool/Module%20Files/#third-party-only-configs","title":"Third Party Only Configs","text":"<p><code>AdditionalLibs = {String List}</code> - Additional Libraries to append</p> <p><code>CommandToRun = {String List}</code> - The command to run for third party</p>"},{"location":"Tools%20and%20Programs/RelightBuildTool/Other%20Modes/","title":"Other Modes","text":""},{"location":"Tools%20and%20Programs/RelightBuildTool/Other%20Modes/#clean-mode","title":"Clean Mode","text":"<p>Can be accessed using <code>-Mode=clean</code></p> <p>Clean mode cleans out both intermediate and binary directories</p> <p>For all options, Click here</p>"},{"location":"Tools%20and%20Programs/RelightBuildTool/Running%20RelightBuildTool/","title":"Running RelightBuildTool","text":""},{"location":"Tools%20and%20Programs/RelightBuildTool/Running%20RelightBuildTool/#requirements","title":"Requirements","text":"<ul> <li>Python 3</li> <li>colorama 0.4.4</li> </ul>"},{"location":"Tools%20and%20Programs/RelightBuildTool/Running%20RelightBuildTool/#running-rbt","title":"Running RBT","text":"<p>To run the program, you must run the python runtime, The program iteself is located at <code>Programs/RelightBuildTool</code>, and you must run main.py in that directory</p> <p>If you are running the program through a terminal, the command you run would be:</p> <pre><code>Python main.py {Arguments}\n</code></pre>"},{"location":"Tools%20and%20Programs/RelightBuildTool/Running%20RelightBuildTool/#command-line-arguments","title":"Command line arguments","text":""},{"location":"Tools%20and%20Programs/RelightBuildTool/Running%20RelightBuildTool/#global-commands-can-be-used-in-any-modes","title":"Global commands (can be used in any modes)","text":"<p><code>-Mode={String}</code> - The mode we will run, if left empty, we will run Build mode by default. (for options and information on modes, check Other Modes)</p> <p><code>-Project={String}</code> - The full path of the .RProject file that will be compiled</p> <p><code>-Target={String}</code> - The file name of the target file without extension (Only used when <code>-Project</code> is not defined)</p> <p><code>-TargetDir={String}</code> - The directory of the target file to search (Only used when <code>-Project</code> is not defined)</p> <p><code>-Platform={String}</code> - The platform and SDK we will compile/use. We will use developer's platform by default</p> <p><code>-Arch={String}</code> - The architecture we will compile/use. Default is x86</p> <p><code>-BuildType={String}</code> - The type of build we will compile (Final, Development, or Debug), default is Development</p> <p><code>-NoMessages={Boolean}</code> - If true, then we will not print log details in the terminal. Default is false</p> <p><code>-NoLog={Boolean}</code> - If true, we will not log information into a text file. Default is false</p>"},{"location":"Tools%20and%20Programs/RelightBuildTool/Running%20RelightBuildTool/#build-commands","title":"Build commands","text":"<p><code>-Module={String}</code> - The modules to compile, even if they are not defined in Target file</p> <p><code>-Precompile={Boolean}</code> - If true, then we will use existing modules instead of compiling the engine from scratch. Default is false</p> <p><code>-Cook={Boolean}</code> - If true, then we will automatically run RelightCookerTool with default parameters while building. Default is false (for more imformation, check RelightCookerTool)</p>"},{"location":"Tools%20and%20Programs/RelightBuildTool/Running%20RelightBuildTool/#clean-commands","title":"Clean commands","text":"<p><code>-Ignore-Bin={Boolean}</code> - If true, we will ignore the bin directory when cleaning. Default is false</p> <p><code>-Ignore-Intermediate={Boolean}</code> - If true, we will ignore the intermediate directory when cleaning. Default is false</p> <p><code>-Ignore-Cooked={Boolean}</code> - If true, we will ignore the Cooked directory (Configs, content, shaders, etc) when cleaning. Default is false</p> <p>Dangerous clean options</p> <p>The following clean mode arguments are dangerous and can delete important files without warning. Only use at your own risk!</p> <p><code>--Ignore-All-Confirmation={Boolean}</code> - If true, then we will only ask for confirmation once, and will never ask again. Default is false</p> <p><code>--Verify-Sus-Directories={Boolean}</code> - instead of closing RBT when encountering a suspicious directory to delete, we will instead ask the user if they wish to continue. Default is false</p> <p><code>--Clean-Everything={Boolean}</code> - Delete all files in a directory instead of specified extensions. Default is false</p>"},{"location":"Tools%20and%20Programs/RelightBuildTool/Running%20RelightBuildTool/#building-the-engine","title":"Building the engine","text":"<p>Scripts for common platforms to build the engine is located in the <code>Build</code> directory of Relight Engine. If you wish to build the engine from scratch, then you would run RBT on the Relight Engine target file. An example of the command is the following:</p> <pre><code>python main.py -Target=RelightEngine -TargetDir=../../ -Platform=Linux -Arch=x86 -Cook=True\n</code></pre>"},{"location":"Tools%20and%20Programs/RelightBuildTool/Target%20Files/","title":"Target Files","text":"<p>malicious code risk</p> <p>Target files can contain code that could harm your computer, since it uses python as a backend. Please check and verify any target files you see online</p> <p>Target files (<code>.Target</code>) are files that acts as a configuration for the final binary output (executable or dynamic library). All configuration in this target will represent everything in the executable itself.</p>"},{"location":"Tools%20and%20Programs/RelightBuildTool/Target%20Files/#syntax","title":"Syntax","text":"<p>Target files internally uses python, meaning all syntax and libraries are the exact same as python</p>"},{"location":"Tools%20and%20Programs/RelightBuildTool/Target%20Files/#read-only-configs","title":"Read-only configs","text":"<p>The following are settings that cannot be set by the target file, but can be read, allowing for more personalized settings</p> <p><code>TARGET_NAME</code> - A string that contains the name of the target</p> <p><code>TARGET_DIRECTORY</code> - A String that contains the directory of the target</p> <p><code>PROJECT</code> - A String that contains the full project name and directory</p> <p><code>PLATFORM</code> - A String that contains the platform we are compiling on</p> <p><code>PLATFORM_GROUP</code> - A String that contains the platform group we are compiling on</p> <p><code>FILEPATH</code> - A String that contains the full file path of the target file</p>"},{"location":"Tools%20and%20Programs/RelightBuildTool/Target%20Files/#target-configs","title":"Target Configs","text":"<p>Configuration that will control the final execuatble. The following is all the options you can set</p>"},{"location":"Tools%20and%20Programs/RelightBuildTool/Target%20Files/#universial","title":"Universial","text":"<p><code>Name = {String}</code> - The name of the target, will be used as the executable name (unless project file or command argument overrides it). If blank or none, we will use the target file name instead</p> <p><code>TargetType = {String}</code> - The target type, can be either <code>Game</code>, <code>Editor</code>, <code>Client</code>, <code>Server</code>, or <code>Program</code>. Used for enabiling some settings</p> <p><code>BuildType = {String}</code> - The Build type, determines what debugging tools we should include, can be either <code>Debug</code>, <code>Development</code>, and <code>Final</code></p> <p><code>LinkType = {String}</code> - The type of link, if it\u2019s <code>Monolithic</code>, it\u2019s all in the same executable, if it\u2019s <code>Modular</code>, then each module is a dynamic library alongside the executable. The default alue is <code>Default</code> (which will switch between the modes depending on the project type)</p> <p><code>IntermediateType = {String}</code> - The intermediate type, if it's <code>Shared</code>, then all Engine code would be in engine intermediate, while <code>Unique</code> is for Everything, even Engine code, to be in project intermediate (which will switch between the modes depending on the project type)</p> <p><code>Modules = {String List}</code> - All additional dependencies for the target to compile</p> <p><code>DisableDebugInfo = {Boolean}</code> - If true, will not create any debug related files. Default is false</p> <p><code>IsDynamicLibrary = {Boolean}</code> - If true, then instead of compiling this target as an executable, we compile it as a library. Default is false</p> <p><code>CompileEditor = {Boolean}</code> - If true, we will compile the editor, otherwise we do not need to compile the editor. Default is false</p> <p><code>UseCompiledEngine = {Boolean}</code> - If true, we will use existing Engine static libraries via intermediate directories instead of compiling the engine each time. Default is false</p> <p><code>Unity = {Boolean}</code> - If true, we will use the UNITY system, which will combine all .cpp files in each module into a single file. Default is true</p> <p><code>Defines = {String List}</code> - Defines that will be spread throughout every module</p> <p><code>IncludeLaunch = {Boolean}</code> -  If true, then we will include the module that launches the application, otherwise the first included module listed in <code>Modules</code> will be used as launcher instead. Default is false</p> <p><code>LaunchName = {String}</code> - If IncludeLaunch is true, then this would be the module name that would launch the application, default is <code>Launch</code></p> <p><code>ExtraCompileArgs = {String}</code> - Any additional arguments to pass when compiling</p> <p><code>ExtraLinkingArgs = {String}</code> - Any additional arguments to pass when linking</p> <p><code>LinkFilesTogether = {Boolean}</code> - If true, we will link object files into final binary, if false, we will just output the object files. Default is true</p> <p><code>Arch = {String}</code> - The arch to compile if none is defined, default is \"x86\"</p> <p><code>CopyIncToIntermediate = {Boolean}</code> - if true, we will copy all header files to the intermediate folder. This will be used mostly for precompiled engine binaries. Default is False</p> <p><code>Precompiled = {Boolean}</code> - If true, then any engine module will use precompiled binaries. Musch faster for those who are not modifying Engine Code. Default is true</p> <p><code>AlwaysCompileThirdParty = {Boolean}</code> - If true, we will skip building Third party dependencies and assume the Third Party is already compiled. Default is false</p> <p><code>UnityMinSourceFiles = {Integer}</code> - The amount of source files needed to use Unity files, requires Unity to be true. Leave 0 to always use Unity. Default is 0</p> <p><code>ToolchainOverride = {NONE}</code> - A toolchain we will override</p> <p><code>BinSubPaths = {NONE}</code> - if not empty, we will put the binaries in a subfolder</p>"},{"location":"Tools%20and%20Programs/RelightBuildTool/Target%20Files/#linux","title":"Linux","text":"<p><code>UseAddressSanitizer = {Boolean}</code> - If we should use Address Sanitizer. Default is false</p> <p><code>UseThreadSanitizer = {Boolean}</code> - If we should use Thread Sanitizer. Default is false</p> <p><code>UseUnknownSanitizer = {Boolean}</code> - If we should use Undefined/Unknown Sanitizer. Default is false</p> <p><code>SavePSYM = {Boolean}</code> - If true, we will save portable symbol file. Default is false</p>"},{"location":"Tools%20and%20Programs/RelightBuildTool/Using%20a%20Custom%20SDK/","title":"Using a Custom SDK","text":"<p>SDK in the context of Relight Engine is a peice of software(s) that allows RelightBuildTool to build the engine and project to a specified platform. SDK's can support binary and other python files that are needed to compile to a specific platform</p>"},{"location":"Tools%20and%20Programs/RelightBuildTool/Using%20a%20Custom%20SDK/#all-valued-locations","title":"All valued locations","text":"<p>Official SDK created by the Relight Team are located in the <code>Programs/RelightBuildTool/SDK</code>, all unofficial SDKs are located at <code>Extras/CustomSDK</code></p>"},{"location":"Tools%20and%20Programs/RelightBuildTool/Using%20a%20Custom%20SDK/#getting-the-template-and-using-it-as-a-base","title":"Getting the template and using it as a base","text":"<p>The SDK Template is located at <code>Extras/CustomSDK/Example/</code>. This contains documentation and examples of all important functions needed to compile the engine/project to the desired platform. The platform name must be in front of any other text (for example, if the platform is System, then the file names should be <code>SystemPlatform</code>, <code>SystemPlatformSDK</code>, etc).</p>"},{"location":"Tools%20and%20Programs/RelightBuildTool/Using%20a%20Custom%20SDK/#all-required-files","title":"All required files","text":"<p><code>PlatformSDK</code> - Checks for requirements for the SDK, some actions include validating SDK files, checking if SDK is destructive, etc.</p> <p><code>Platform</code> - Holds configurations and definitions for the platforms, example includes binary extensions, if we should create debug info, etc</p> <p><code>PlatformFactory</code> - Interface for RBT. Allows RBT to interact with the rest of the required files, registers SDK and platform class.</p> <p><code>Toolchain</code> - handles the process of compiling and linking c/cpp files together, uses Actions</p>"},{"location":"Tools%20and%20Programs/RelightBuildTool/Using%20a%20Custom%20SDK/#using-action-in-toolchain","title":"Using Action in Toolchain","text":"<p>in the Toolchain class, developers is expected to use Actions. Actions are objects that store the commands to run.</p> <pre><code>NewAction = Action.Action()\n</code></pre> <p>This creates a new Action class</p> <pre><code>Action.CommandPath = {String}\n</code></pre> <p>this will set the main program to run (example <code>'/home/software/CoolPlatformSDK/CoolPlatformSDK.bin'</code></p> <pre><code>Action.Arguments = {String}\n</code></pre> <p>All arguments we will run with the program (example <code>'-Compile all -SetCoolStuff=false'</code>)</p>"},{"location":"Tools%20and%20Programs/RelightBuildTool/Using%20a%20Custom%20SDK/#compile-environment-class","title":"Compile Environment Class","text":"<p>The Compile Environment holds all configuration for the Compiler and Assembler. These will be defined during the RBT build pipeline, and can be used by the toolchain to read configuration and apply them</p> <p><code>Plat = {Platform enum}</code> - The platform we are compiling (note: pre-defined in RBT, may not be needed)</p> <p><code>Conf = {None}</code> - What debugging mode we will use</p> <p><code>PCH_Act = {CompileEnvironment.PCHAction}</code> - The action we will use for PCH files. Default is Null</p> <p><code>Out = {CompileEnvironment.Output}</code> - All output info</p> <p><code>Arch = {String}</code> - The CPU architecture</p> <p><code>SharedPCH = {String List}</code> - The Precompiled Header file we will use globally</p> <p><code>PCHIncludeName = {String}</code> - The name of the header file we will precompile</p> <p><code>UseSharedBuildEnv = {Boolean}</code> - Create shared environment. Default is false</p> <p><code>UseAVX = {Boolean}</code> - If we should use Advanced Vector Extensions. Default is false</p> <p><code>UseRTTI = {Boolean}</code> - If we should use Run-Time Type Information. Default is false</p> <p><code>UseInlining = {Boolean}</code> - If we should use function inlining. Default is false</p> <p><code>BufferSecurityChecks = {Boolean}</code> - If we should use buffer security checks. Default is true</p> <p><code>FalseUnityOverride = {Boolean}</code> - Use if it's faster to not use UNITY system, will disable UNITY even if it's on. Default is false</p> <p><code>MinUnitySource = {Integer}</code> - The amount of files needed to enable Unity. Default is 0</p> <p><code>MinPCHSource = {Integer}</code> - The amount of files needed before we use Precompiled Header. Default is 0</p> <p><code>BuildLocallySNDBS = {Boolean}</code> - Build Locally when using SN-DBS. Default is false</p> <p><code>ExceptionHandling = {Boolean}</code> - If we should do Exception Handling. Default is false</p> <p><code>ShadowVariableWarnings = {Boolean}</code> - If we should give warnings about Shadow Variables. Default is true</p> <p><code>ShadowVariableAsError = {Boolean}</code> - If we shall give an error if there's a shadow variable. Default is false</p> <p><code>UndefinedIdentifierWarnings = {Boolean}</code> - If we should give warnings about Undefined Identifiers. Default is true</p> <p><code>UndefinedIdentifierAsError = {Boolean}</code> - If we shall give an error if there's an Undefined Identifiers. Default is false</p> <p><code>Optimize = {Boolean}</code> - If true, we will optimize the binary. Default is false</p> <p><code>OptimizeSize = {Boolean}</code> - If true, we will optimize for smallest possible size. Default is false</p> <p><code>AddDebugInfo = {Boolean}</code> - If we should create Debug information. Default is true</p> <p><code>IsLibrary = {Boolean}</code> - If we are compiling static library (.a/.lib). Default is false</p> <p><code>IsDynamic = {Boolean}</code> - If we are compiling dynamically (.so/.dll). Default is false</p> <p><code>UseStaticCRT = {Boolean}</code> - If we should compile using statically-linked CRT. Default is false</p> <p><code>UseDebugCRT = {Boolean}</code> - If we should use Debug CRT. Default is false</p> <p><code>ExcludeFramePointers = {Boolean}</code> - If true, we shall not include frame pointers. Default is true</p> <p><code>IncrementalLinking = {Boolean}</code> - If true, we shall only link files that are updated. Default is true</p> <p><code>AllowLTCG = {Boolean}</code> - If we should use link time code generation. Default is false</p> <p><code>PGOProfile = {Boolean}</code> - If we should use Profile Guided Optimization Instrumentation. Default is false</p> <p><code>PGOOptimize = {Boolean}</code> - If we should optimize using Profile Guided Optimization. Default is false</p> <p><code>PGODirectory = {String}</code> - Directory where PGO is stored</p> <p><code>PGOFilePrefix = {String}</code> - Filename where PGO is stored</p> <p><code>PrintTimingInfo = {Boolean}</code> - If true, we will log timing info from the compiler we are using. Default is false</p> <p><code>GenerateDependFile = {Boolean}</code> - If true, we shall Put Dependencies file along with output build products. Default is true</p> <p><code>AllowRemotelyCompiledPCHs = {Boolean}</code> - If true, we shall let XGE compile PCH files externally, instead of locally. Default is false</p> <p><code>UserIncPaths = {String List}</code> - Included paths</p> <p><code>SysIncPaths = {String List}</code> - Included System paths, will supress warnings</p> <p><code>CheckSysHeaderForChanges = {Boolean}</code> - If headers in SysIncPaths are modified, then the action is outdated. Default is false</p> <p><code>ForceIncFiles = {String List}</code> - Paths to include no matter what</p> <p><code>Defines = {String List}</code> - Definitions we will use across the engine</p> <p><code>AdditionalArgs = {String}</code> - Any additional arguments we will use</p> <p><code>AdditionalFrameworks = {String List}</code> - Any additional Frameworks we will use</p> <p><code>PCHFile = {None}</code> - The Precompiled Header file</p> <p><code>UsingRHT = {Boolean}</code> - If we are using Relight Header Tool. Default is false</p> <p><code>HideSymbols = {Boolean}</code> - If we should hide Symbols by default. Default is false</p> <p><code>CopyIncToIntermediate = {Boolean}</code> - If we should copy header files to intermediate. Default is false</p>"},{"location":"Tools%20and%20Programs/RelightBuildTool/Using%20a%20Custom%20SDK/#linker-environment-class","title":"Linker Environment Class","text":"<p>The Link Environment holds all configuration for the Linker. These will be defined during the RBT build pipeline, and can be used by the toolchain to read configuration and apply them</p> <p><code>Platform = {None}</code> - The platform we are compiling</p> <p><code>Config = {None}</code> - The config we are compiling</p> <p><code>Arch = {String}</code> - The arch we are linking</p> <p><code>BundleDir = {String}</code> - Bundle software path on MacOS</p> <p><code>OutputDir = {String}</code> - The directory that will store non-binary data in</p> <p><code>IntermediateDir = {String}</code> - The intermediate directory</p> <p><code>LocalShadowDir = {None}</code> - The local shadow directory, this directory serves as a temp directory of source files, mainly used when linking via mutliple servers</p> <p><code>OutputPaths = {String List}</code> - The full output binary files and it's path that this linker will produce</p> <p><code>LibraryPaths = {String List}</code> - Paths to search for libraries</p> <p><code>ExcludeLibs = {String List}</code> - Libraries we will not link</p> <p><code>AdditionalLibs = {Stirng List}</code> - Additional Libraries we will link</p> <p><code>RuntimeLibPaths = {String List}</code> - Paths to search for Runtime Libraries</p> <p><code>AdditionalFrameworks = {Stirng List}</code> - Additional framworks to link</p> <p><code>AdditionalBundlesRes = {String List}</code> - Additional bundles to link</p> <p><code>DelayLoadDynamics = {String List}</code> - A list of \"Delayed Load Dynamics\". These are Dynamic libraries that will not load into the software until they are first called</p> <p><code>AdditionalArgs = {String}</code> - Additional arguments to pass</p> <p><code>AddDebugInfo = {Boolean}</code> - If we should create debug information. Default is true</p> <p><code>DisableSymbolCashe = {Boolean}</code> - If true, we shall not create cached symbols. Default is false</p> <p><code>IsBuildingLibrary = {Boolean}</code> - If true, then we are linking to a static Library (.a, .lib, etc). Default is false</p> <p><code>IsBuildingDynamic = {Boolean}</code> - If true, then we are linking to a dynamic Library (.so, .dll, etc). Default is false</p> <p><code>DefaultStackSize = {Integer}</code> - Default memory size allocation. Default is 5000000</p> <p><code>OptimizeSize = {Boolean}</code> - If True, the software will be optimize for size. Default is false</p> <p><code>ExcludeFramePointers = {Boolean}</code> - If true, we will not include frame pointers. Default is true</p> <p><code>IncrementalLinking = {Boolean}</code> - If true, then we will use Incremental Linking. A system which modifies the Output executable instead of rebuilding everything from scratch. Default is false</p> <p><code>AllowLTCG = {Boolean}</code> - If true, then we will use LTCG (Link Time Code Generation). Default is false</p> <p><code>PGOProfile = {Boolean}</code> - If true, then we will use PGO Profile (Profile Guided Optimization). Default is false</p> <p><code>PGOOptimize = {Boolean}</code> - If true, then we will use PGO Optimize. Default is false</p> <p><code>PGODirectory = {String}</code> - Directory that PGO Profiling will be saved</p> <p><code>PGOFilePrefix = {String}</code> - File prefix for the PGO Profiling, usually platform specific</p> <p><code>CreateMapFile = {Boolean}</code> - If true, we will attempt to create map file, which stores detailed overview about the linker. Default is false</p> <p><code>AllowASLR = {Boolean}</code> - If true, we will attempt ASLR (address space layout randomization). Default is false</p> <p><code>UseFastPDBLinking = {Boolean}</code> - If we should use Fast PDB when linking. Default is false</p> <p><code>PrintTimingInfo = {Boolean}</code> - Print Timing info when linking. Default is false</p> <p><code>InputFiles = {String List}</code> - A list of files that will be linked</p> <p><code>InputLibs = {String List}</code> - Libraries we will link</p>"},{"location":"Tools%20and%20Programs/RelightBuildTool/Behind%20the%20Scenes/","title":"Index","text":"<p>The following will explain everything of the internals of how RelightBuildTool works</p> <p>How C/C++ compiling works - Learn how a typical C/C++ compiling works, understanding this topic will explain many terminology and context for many functions in RBT</p> <p>Building Pipeline - Learn each step of how RBT internally builds an application through all it's building process</p> <p>Unity System - Learn how UNITY combines all C/C++ files into a universial file</p>"},{"location":"Tools%20and%20Programs/RelightBuildTool/Behind%20the%20Scenes/Building%20Pipeline/","title":"Building Pipeline","text":""},{"location":"Tools%20and%20Programs/RelightBuildTool/Behind%20the%20Scenes/Building%20Pipeline/#overview-on-building-pipeline","title":"Overview on Building pipeline","text":"<p>Here's a graphical overview of the pipeline RBT goes through when building a project. We will go over the details for each step in each section</p> <pre><code>graph TD\n    Main(Main.py) --&gt;|Run Build Mode| BuildMode;\n    BuildMode[BuildMode.py] --&gt;|Get Target File| TargetBuilder;\n    TargetBuilder[TargetBuilder.py] --&gt;|create class representing binary| Binary;\n    Binary[Binary.py] --&gt;|Compile module in binary| ModuleBuilder;\n    ModuleBuilder[ModuleBuilder.py] --&gt;|Run module in Toolchain| CompileToolchain;\n    CompileToolchain[SDK Toolchain Compiling process];\n\n    CompileToolchain --&gt;|Loop for each module| Binary;\n\n    LinkingToolchain[SDK Toolchain Linking process];\n    CompileToolchain --&gt;|When binaries compiled, link them| LinkingToolchain;\n\n    LinkingToolchain --&gt;|For each binary| TargetBuilder;\n\n    LinkingToolchain --&gt;|After we compiled all targets| ActionExecute;\n    ActionExecute[ActionExecute.py] --&gt;|Run action execute| StartActionExe;\n    StartActionExe[Start Action Executor] --&gt;|Get action| ExecuteAction;\n    ExecuteAction[Execute action] --&gt;|For each Action in list| ActionExecute;\n\n    LinkingToolchain --&gt;|For each Target file| BuildMode;\n\n    ExecuteAction --&gt;|After all target's actions are completed| Finish;\n\n    Finish(Finished);</code></pre>"},{"location":"Tools%20and%20Programs/RelightBuildTool/Behind%20the%20Scenes/Building%20Pipeline/#mainpy","title":"main.py","text":"<p><code>main.py</code> is the first file to run and act as a precondition to build mode, all it currently does is print the logo, set up the logging system, and create the build mode class, once the class has been created, we will run the <code>main</code> function for the mode</p>"},{"location":"Tools%20and%20Programs/RelightBuildTool/Behind%20the%20Scenes/Building%20Pipeline/#buildmodepy","title":"BuildMode.py","text":"<p>When the main function of BuildMode is called, we will create a <code>StartingTarget</code>, a Starting target represents the first target we will build with minimum information, this usually get's set by the <code>command line arguments</code>. After this, we will register the specified platform to the platform class, and start the <code>Build Process</code>. when we are in the build process, we will get a list of targets and for each target we will Create and Build TargetBuilder. After we build each target, we will get the actions from the target and add it to a <code>Action List Manager</code>, which will link all actions together. After this, we will ensure all starting target has the same config, then we will execute the actions</p>"},{"location":"Tools%20and%20Programs/RelightBuildTool/Behind%20the%20Scenes/Building%20Pipeline/#targetbuilderpy","title":"TargetBuilder.py","text":"<p>TargetBuilder represents the building process of a program, including multiple binaries and their configuration. when the Create function is called, we will create a <code>TargetReader</code> class, which will be found based on the name. Once we created the reader, we will create the TargetBuilder class itself, when we create the class, we set the starting target and the normal target, then we will set the arch and engine and project intermediate directory, after this, then we will setup prebuild. Prebuild will get each module and set values. After the creation of the TargetBuilder, we will start the <code>Build process</code>. When the build function get's called, the first step is to run the <code>RelightCookerTool</code> if we enabled the auto baking to be on. Next, we get the platform class, set up the <code>Toolchain class</code>, create both link and compile environment and set global settings for them, and get all values needed to create and run the build function in the Binary class. binary filter, plugin support, and Header Tool is also set here</p>"},{"location":"Tools%20and%20Programs/RelightBuildTool/Behind%20the%20Scenes/Building%20Pipeline/#binarypy","title":"Binary.py","text":"<p>Binary represents a specific binary file to build and settings for the specific binary. When first created, it will just set all the required values into the class instance. Then in the Build function, we will run CreateLinkEnv. Inside CreateLinkEnv, we will create a new link and compile environment based on the input environments, and modify the environments based on settings. After this, we will loop for each module in a binary, we will add each module's additional libs to the link environment's additional lib, after that, we will compile the module, sync NewLinkEnv with CompileEnv, and add the input files to the NewLinkEnv. After syncing more LinkEnv, we will return it to the NewLinkEnv to the Build function. After we finished the NewLinkEnv function, we will continue to linking if we are allowed to, and run LinkEveryFiles in toolchain class. After this, we will run PostBuilt from toolchain and return the output files</p>"},{"location":"Tools%20and%20Programs/RelightBuildTool/Behind%20the%20Scenes/Building%20Pipeline/#modulebuilderpy","title":"ModuleBuilder.py","text":"<p>ModuleBuilder represents the compiling process of a specific module, and is main purpose is handling configuration on a module and compiling it, please note that ModuleBuilder does not handle anything relating to linking, and only in the compiling process. Once we start the compilation function, we will check for any 3rd party and if there is any, build it if we are allowed. After this, we will add all additionallibs to the return list, and add Third party contents to the <code>CompileEnv</code>. Next we will run <code>GetAndCompileDependencies</code>, which first add the include to the module's src directory to the compile environment. After this, we will run each module, then loop for each submodule in that module, and compile each one, ensure all requirements are compiled in order. Back to the compile function, after we confirm all required modules are compiled, we will setup the UNITY SYSTEM if it's activated. We will then set the Module Name from the reader, and set the Intermediate directory. If Precompiled is false and we are not using UNITY SYSTEM, then we will run CompileMultiArchCPPs from Toolchain, if UNITY SYSTEM is true, then we will run UniteCPPFiles from UNITY SYSTEM. If we are using Precompiled mode, then get all engine object files and get includes from intermediate, and compile/unify the project files. We will return the list of compiled objects that will be created</p>"},{"location":"Tools%20and%20Programs/RelightBuildTool/Behind%20the%20Scenes/Building%20Pipeline/#toolchain","title":"Toolchain","text":"<p>The Toolchain process depends on each platform, for documentation and tutorial on how the SDK works, including Toolchain, then please see our SDK Tutorial Document, you can also view a template from <code>Extras/CustomSDK/Examples</code></p>"},{"location":"Tools%20and%20Programs/RelightBuildTool/Behind%20the%20Scenes/Building%20Pipeline/#actionexecutepy","title":"ActionExecute.py","text":"<p>Once the toolchain set's all actions and added them to the list, BuildMode.py will start ActionExecute, in this class, we will get each action and execute the commands to compile. We will keep track of the progress on each action and execute them, compiling and linking all files and create the final product</p>"},{"location":"Tools%20and%20Programs/RelightBuildTool/Behind%20the%20Scenes/How%20Compiling%20Works/","title":"How Compiling Works","text":"<p>C++ compiling context</p> <p>This goes over C/C++ compiling pipeline, if you already know how it works, you may skip this section</p>"},{"location":"Tools%20and%20Programs/RelightBuildTool/Behind%20the%20Scenes/How%20Compiling%20Works/#basic-pipeline-overview","title":"Basic pipeline overview","text":"<pre><code>graph TB\n    A_Step1[File1.cpp] --&gt;|Preprocessor| A_Step2;\n    B_Step1[File2.cpp] --&gt;|Preprocessor| B_Step2;\n    C_Step1[File3.cpp] --&gt;|Preprocessor| C_Step2;\n\n    A_Step2[File1.cpp] --&gt;|Compiler| A_Step3;\n    B_Step2[File2.cpp] --&gt;|Compiler| B_Step3;\n    C_Step2[File3.cpp] --&gt;|Compiler| C_Step3;\n\n    A_Step3[File1.s] --&gt;|Assembler| A_Step4;\n    B_Step3[File2.s] --&gt;|Assembler| B_Step4;\n    C_Step3[File3.s] --&gt;|Assembler| C_Step4;\n\n    A_Step4[File1.o] --&gt;|Linker| Exe;\n    B_Step4[File2.o] --&gt;|Linker| Exe;\n    C_Step4[File3.o] --&gt;|Linker| Exe;\n\n    Exe[Final Binary file];</code></pre>"},{"location":"Tools%20and%20Programs/RelightBuildTool/Behind%20the%20Scenes/How%20Compiling%20Works/#preprocessor","title":"Preprocessor","text":"<p>The Preprocessor modifies a C, C++, and/or Header file and store the modifications in a copy to be used in pipeline. These modifications are included by the developers themselves. For example, if we have a file that looks like</p> <pre><code>#include \"LogStuff.h\"\n#define Greeting \"Hello world!\"\n\nint main()\n{\n    Log(Greeting);\n}\n</code></pre> <p>Then the Preprocessor copy would look like this:</p> <pre><code>void Log(char* Input)\n{\n    printf(Input);\n}\n\nint main()\n{\n    Log(\"Hello world!\");\n}\n</code></pre> <p>This is especially useful in the Engine for includes and constant compiled variables, such as build configurations and platform info, without having the engine checking the info at runtime.</p>"},{"location":"Tools%20and%20Programs/RelightBuildTool/Behind%20the%20Scenes/How%20Compiling%20Works/#compiler","title":"Compiler","text":"<p>The compiler converts the Preprocessed C/C++ files into assembly. Assembly is a low-level language, which acts as a readable version of machine code. Each machine code is different depending on your architecture, and can be difficult to read without comments. RelightBuildTool currently doesn't interact with the compiler, if at all, outside of any SDK toolset. Let's show an example, if your .cpp file looks like this</p> <pre><code>int main()\n{\n    int Number = 5;\n}\n</code></pre> <p>Then the Compiler will turn into this (using <code>GCC on x86_64</code>) <pre><code>main:\n    push    rbp\n    mov     rbp, rsp\n    mov     DWORD PTR [rbp-4], 5\n    mov     eax, 0\n    pop     rbp\n    ret\n</code></pre></p> <p>all generated assembly code are located in <code>.s</code> files</p>"},{"location":"Tools%20and%20Programs/RelightBuildTool/Behind%20the%20Scenes/How%20Compiling%20Works/#assembler","title":"Assembler","text":"<p>After we get the assembly code, it's time to assemble it into machine code, this process turns all <code>.s</code> files into native machine code objects <code>.o</code> (<code>.obj</code> on windows). These object files resides in the <code>Intermediate</code> directory and can be viewed yourself if you go into this directory for your compiled project. These files are NOT shipped in the final product, and mainly served as to track for updates, ensuring we don't recompile code that hasn't been updated.</p>"},{"location":"Tools%20and%20Programs/RelightBuildTool/Behind%20the%20Scenes/How%20Compiling%20Works/#linker","title":"Linker","text":"<p>After we get each object files, it's time to link them together into the final product, this works by combining all objects into a single binary file. There are 2 different types of linking a developer could pick</p> <p><code>Static Linking</code> - Combining object and static libraries (<code>.a</code> or <code>.lib</code>) into a single executable</p> <p><code>Dynamic Linking</code> - Have references to code in another file (<code>.so</code> or <code>.dll</code>)</p> <p>Most of the time, Dynamic Linking will be a hybrid of sort, using Static Linking for some libraries, and using dynamic linking for others, or a dynamic library may have static library including in the file. No matter how you look at it, this is the final step, as after this, the final executable is produced</p>"},{"location":"Tools%20and%20Programs/RelightBuildTool/Behind%20the%20Scenes/Unity%20System/","title":"Unity System","text":"<p>The unity system is a process in which each <code>.cpp</code> file is combined into a single <code>.cpp</code> file before compiling. This is useful for faster compile times and compiling preformance</p> <pre><code>graph TD\nFile1(File1.cpp) --&gt;|Unity System| Unified;\nFile2(File2.cpp) --&gt;|Unity System| Unified;\nFile3(File3.cpp) --&gt;|Unity System| Unified;\nUnified(Module.0.cpp) --&gt;|Get's automatically added to Pipeline| Pipeline;\nPipeline(RelightBuildTool Pipeline);</code></pre> <p>Pros:</p> <ul> <li>Faster compiling times</li> <li>Iterate's better on large projects</li> </ul> <p>Cons:</p> <ul> <li>Harder to debug</li> <li>Large object files</li> </ul>"}]}